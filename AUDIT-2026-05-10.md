# Audit 2026-05-10 — crepesforecast

## Auto-fixed (this run)
- [0cd2aba] audit: add OpenGraph/Twitter/canonical metadata + longer description, scaffold `app/robots.ts` + `app/sitemap.ts` (Next.js App Router conventions). Description was 91 chars → ~190 chars in the recommended 120–200 band; OG/Twitter cards now present; canonical set to `/`; `metadataBase` configured from `NEXT_PUBLIC_SITE_URL`.

## TODO — manual attention

### Security
- **2 moderate npm advisories** (postcss XSS via Unescaped `</style>`, GHSA-qx2v-qp2m-jg93, CVSS 6.1) reaching prod through `next` → `postcss`. `npm audit` reports `fixAvailable: next@9.3.3` which is a major-version DOWNGRADE from current `next@^16.2.4` — not a real fix and not safe to apply. Requires waiting for Next.js to bump its bundled postcss; track upstream advisory.
- No HIGH/CRITICAL vulns. No hardcoded secrets found. `.env*.local` properly ignored. `.env.example` contains only placeholders (no real keys).
- `app/api/email-briefing/route.ts:15` — when `CRON_SECRET` is unset the endpoint is **open**. Comment says "no secret configured → open (local dev)" but this also means an unset env var in prod silently disables auth. Recommend hard-failing with 503 in prod when `CRON_SECRET` is missing (not auto-fixed: behavior change).

### SEO
- ✅ Now have: title (40 chars), description (~190 chars), OG, Twitter card, canonical, `<html lang="fr">`, robots.txt, sitemap.xml.
- No JSON-LD structured data (e.g. `LocalBusiness` schema with caravan location + opening hours). Worth adding for local search; not auto-fixed because it requires deciding on a schema and exact opening-hours expression.
- Single-page site, so sitemap currently has only `/`. Fine for now.

### Content quality
- All copy in French (consistent with CLAUDE.md). No 2024-or-earlier dates found in UI strings.
- Only external link is footer `https://diyfunproject.com`. No internal route links found, so no broken-link surface.
- `weekly-emails/2026-04-27.html` is the most recent archived email (2 weeks old as of 2026-05-10). Cron schedule (`0 7 * * 5,6,0`) should keep this populated automatically.

### Bugs
- `npm run lint` is **broken**: `next lint` was deprecated in Next.js 15 and removed in 16. Currently produces "Invalid project directory provided, no such directory: /home/user/crepesforecast/lint". Migrate to ESLint CLI directly (e.g. `eslint .`) or remove the script. Not auto-fixed (requires deciding on ESLint config strategy + adding `eslint`/`eslint-config-next` deps).
- `npm run typecheck`: ✅ passes.
- `npm run build`: ✅ passes. Now generates `/robots.txt` and `/sitemap.xml` static routes.
- `npm test`: ✅ 23/23 tests pass.

### Performance
- **`public/glaceup.png` is 687 KB** — used as the `<BrandLogo />` in the header on every page load. PNG with transparent background; could be a 30–80 KB WebP/AVIF or an SVG (re-render of an existing illustration). Bundled into every cold start. Not auto-fixed (image conversion needs a binary tool / design decision).
- `.next/static` is 1.1 MB total — well under the 2 MB threshold.
- `app/icon.png` (72 KB) and `app/apple-icon.png` (40 KB) are reasonable but could be smaller WebP if you want to squeeze more.
- No blocking `<script>` tags in `<head>`. Fonts use `display: swap`.

### Accessibility
- ✅ All `<img>` tags have non-empty `alt`.
- ✅ Buttons in `Header.tsx` have `type`, `title`, and `aria-selected` for the day-tab role.
- ✅ Tab list has `role="tablist"` + `aria-label="Jour de service"`.
- ✅ Decorative icons use `aria-hidden`.
- ✅ `<html lang="fr">`.
- No form inputs in the app, so labelling is N/A.
- Color contrast not auditable from CLI — verify on the live site at https://crepesforecast.vercel.app/ that the pastel-blue `text-seine-headerInk/60` subtitles meet WCAG AA against the orange-gradient header.

## Scores (1–10, 10 = perfect)
- Security: 8 (2 moderate transitive vulns no fix path, 1 conditional-open auth endpoint)
- SEO: 9 (now has full metadata + sitemap + robots; missing only JSON-LD)
- Content: 9 (clean French copy, no stale dates, single-page so little surface)
- Stability: 9 (typecheck/build/tests all green; lint script broken)
- Performance: 7 (687 KB logo PNG dragged on every load)
- Accessibility: 9 (no obvious gaps; contrast unverified)
- **Overall: 8.5**

## Trend vs last week
No prior `AUDIT-*.md` found in repo — this is the first weekly audit.
